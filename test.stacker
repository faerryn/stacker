: fib dup 1 < if dup 1 switch - fib switch 2 switch - fib + then ;

: tuck switch over ;
: -rot rot rot ;
: fib2 0 1 rot begin -rot tuck + rot 1 switch - dup 0 < until drop drop ;

: cr 10 emit ;
10 fib . cr
10 fib2 . cr

10 begin dup . 1 switch - dup 0 < until cr
